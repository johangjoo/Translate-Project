# LoRA 학습을 위한 필수 패키지

# Unsloth는 별도 설치 필요 (아래 명령어 참고)
# CUDA 12.1+ (RTX 5070 Ti, RTX 40 시리즈): 
#   pip install "unsloth[cu121-torch240] @ git+https://github.com/unslothai/unsloth.git"
# 
# CUDA 11.8:
#   pip install "unsloth[cu118-torch240] @ git+https://github.com/unslothai/unsloth.git"

# 기본 패키지
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
peft>=0.7.0
trl>=0.7.4
bitsandbytes>=0.41.0
accelerate>=0.25.0

# 유틸리티
tqdm>=4.66.0
tensorboard>=2.15.0
sentencepiece>=0.1.99
protobuf>=3.20.0
