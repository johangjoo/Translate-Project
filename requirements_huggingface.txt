# Hugging Face 오디오 노이즈 제거를 위한 필수 라이브러리
speechbrain>=0.5.0
torch>=1.9.0
torchaudio>=0.9.0
librosa>=0.8.0
soundfile>=0.10.0
numpy>=1.19.0

# 선택적 의존성 (GPU 사용 시)
# torch 설치 시 CUDA 버전에 맞는 것을 선택하세요
# 예: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118

# 추가 유용한 라이브러리
matplotlib>=3.3.0  # 시각화용
scipy>=1.6.0       # 신호 처리용
